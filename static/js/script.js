// 简单的JavaScript代码
document.addEventListener('DOMContentLoaded', () => {
    console.log('Web应用已加载完成！');
    
    // 在控制台显示当前时间
    const now = new Date();
    console.log(`当前时间: ${now.toLocaleString()}`);
});
